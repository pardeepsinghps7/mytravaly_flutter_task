name: mytravaly_flutter_task
description: Flutter task for MyTravaly - BLoC architecture, Google Sign-In (frontend mock), hotel search + pagination
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  flutter_bloc: ^8.1.2
  equatable: ^2.0.5
  http: ^1.1.0
  go_router: ^14.2.0
  google_sign_in: ^6.1.4
  flutter_typeahead: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
